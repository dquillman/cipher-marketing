rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Personal dashboard — no auth needed
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
